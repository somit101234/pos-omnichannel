{
  "name": "@pos/electron",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "echo 'Electron dev server'",
    "build": "echo 'Electron build'",
    "lint": "echo 'No linter config yet'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "better-sqlite3": "^9.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
